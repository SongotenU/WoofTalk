---
phase: 38-ar-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WoofTalkAR.xcodeproj/project.pbxproj
  - WoofTalkAR/App.swift
  - WoofTalkAR/ContentView.swift
  - WoofTalkAR/Info.plist
  - Package.swift
requirements:
  - AR-01
autonomous: true
must_haves:
  truths:
    - "Vision Pro project builds successfully in Xcode 16+"
    - "Required entitlements (camera, microphone, motion) configured"
    - "ARKit session configured for world tracking"
    - "RealityKit and SwiftUI integration established"
    - "Swift package dependencies (Supabase) added"
  artifacts:
    - path: "WoofTalkAR.xcodeproj"
      provides: "Xcode project with visionOS target"
      min_lines: 100
    - path: "WoofTalkAR/Info.plist"
      provides: "Entitlements and capabilities configuration"
      contains:
        - "NSCameraUsageDescription"
        - "NSMicrophoneUsageDescription"
    - path: "Package.swift"
      provides: "Swift Package dependencies"
      contains:
        - "Supabase"
  key_links:
    - from: "Package.swift"
      to: "WoofTalkAR/App.swift"
      via: "import supabase"
      pattern: "import Supabase"
    - from: "App.swift"
      to: "ContentView.swift"
      via: "ARView container"
      pattern: "ARView"

---

<objective>
Create a complete visionOS Xcode project with RealityKit integration, proper entitlements, and Swift package dependencies.

**Purpose:** Establish the foundation for all AR development - a properly configured Vision Pro project that can build and run.

**Output:** Xcode project structure with:
- visionOS app target (SwiftUI + RealityKit)
- Entitlements (camera, microphone, motion)
- ARKit session configuration
- Swift packages (Supabase)
- Basic ARView container

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ar-foundation/38-CONTEXT.md
@.planning/phases/38-ar-foundation/38-RESEARCH.md

# Phase 38 Research Context

**Research Summary (from RESEARCH.md):**
- Platform: visionOS with RealityKit + ARKit
- Swift 6, Xcode 16+
- Core ML for dog bark detection
- Supabase Swift SDK for auth
- Fixed position (2m) for Phase 38 bubble

**Key Decisions from CONTEXT.md:**
- Use standard Xcode visionOS app template
- Separate modules: BarkDetection, TranslationAPI, ARExperience, SpatialAudio
- 90 FPS performance target
- Entitlements required: camera, microphone, motion

**Existing Infrastructure:**
- Supabase backend already deployed (Edge Functions ready)
- Edge Function `/v1/translate` expects: human_text, animal_text, source_language, target_language
- No database schema changes needed for Phase 38

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode visionOS project structure</name>
  <files>WoofTalkAR.xcodeproj/project.pbxproj, WoofTalkAR/App.swift, WoofTalkAR/ContentView.swift
  </files>
  <read_first>
    - Read: N/A (creating new project files)
    - Reference: .planning/research/STACK.md for visionOS target settings
  </read_first>
  <acceptance_criteria>
    - Project files exist with correct structure
    - App.swift and ContentView.swift compile without syntax errors
    - ARView configured with WorldTrackingConfiguration
    - RealityKit and ARKit imports present
  </acceptance_criteria>
  <verify>
    <automated>
      test -d WoofTalkAR.xcodeproj
      test -f WoofTalkAR/App.swift
      test -f WoofTalkAR/ContentView.swift
      grep -q "import RealityKit" WoofTalkAR/ContentView.swift
      grep -q "ARView" WoofTalkAR/ContentView.swift
      grep -q "WorldTrackingConfiguration" WoofTalkAR/ContentView.swift
    </automated>
  </verify>
  <action>
    1. Create Xcode project files using `xcodegen` or manual structure:
       - `WoofTalkAR.xcodeproj/project.pbxproj` with:
         * Product bundle identifier: `com.wooftalk.ar`
         * Development team: (use placeholder, user will configure)
         * Target SDK: visionOS 1.0+
         * Supported destinations: Apple Vision Pro
         * Build configurations: Debug, Release
      
    2. Create `WoofTalkAR/App.swift`:
       ```swift
       @main
       struct WoofTalkAR: App {
           var body: some Scene {
               WindowGroup {
                   ContentView()
               }
           }
       }
       ```
      
    3. Create `WoofTalkAR/ContentView.swift`:
       ```swift
       import SwiftUI
       import RealityKit
       import ARKit
       
       struct ContentView: View {
           var body: some View {
               ARContainerView()
                   .edgesIgnoringSafeArea(.all)
           }
       }
       
       struct ARContainerView: UIViewRepresentable {
           func makeUIView(context: Context) -> ARView {
               let arView = ARView(frame: .zero)
               arView.session.run(WorldTrackingConfiguration())
               return arView
           }
           func updateUIView(_ uiView: ARView, context: Context) {}
       }
       ```
      
    4. Set up directory structure:
       - `WoofTalkAR/` (main app directory)
       - `WoofTalkAR/Models/`
       - `WoofTalkAR/Services/`
       - `WoofTalkAR/Views/`
       - `Tests/`
      
    Use exact file paths and ensure RealityKit and ARKit imports are present.
  </action>
  <done>
    - Xcode project files created with valid structure
    - App.swift and ContentView.swift compile syntax-wise
    - ARView configured with WorldTrackingConfiguration
    - Project can be opened in Xcode 16+ without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure entitlements and Info.plist</name>
  <files>WoofTalkAR/Entitlements/WoofTalkAR.entitlements, WoofTalkAR/Info.plist
  </files>
  <read_first>
    - Read: N/A (creating new config files)
    - Reference: .planning/research/STACK.md for required capabilities
  </read_first>
  <acceptance_criteria>
    - Entitlements file contains ARKit, camera, microphone permissions
    - Info.plist contains all usage description keys
    - Project file references entitlements correctly
  </acceptance_criteria>
  <verify>
    <automated>
      test -f WoofTalkAR/Entitlements/WoofTalkAR.entitlements
      test -f WoofTalkAR/Info.plist
      grep -q "com.apple.developer.arkit" WoofTalkAR/Entitlements/WoofTalkAR.entitlements
      grep -q "com.apple.security.device.camera" WoofTalkAR/Entitlements/WoofTalkAR.entitlements
      grep -q "com.apple.security.device.microphone" WoofTalkAR/Entitlements/WoofTalkAR.entitlements
      grep -q "NSCameraUsageDescription" WoofTalkAR/Info.plist
      grep -q "NSMicrophoneUsageDescription" WoofTalkAR/Info.plist
      grep -q "NSMotionUsageDescription" WoofTalkAR/Info.plist
    </automated>
  </verify>
  <action>
    1. Create `WoofTalkAR/Entitlements/WoofTalkAR.entitlements`:
       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
       <plist version="1.0">
       <dict>
           <key>com.apple.developer.arkit</key>
           <true/>
           <key>com.apple.security.device.camera</key>
           <true/>
           <key>com.apple.security.device.microphone</key>
           <true/>
       </dict>
       </plist>
       ```
      
    2. Create `WoofTalkAR/Info.plist`:
       ```xml
       <?xml version="1.0" encoding="UTF-8"?>
       <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
       <plist version="1.0">
       <dict>
           <key>CFBundleDisplayName</key>
           <string>WoofTalk AR</string>
           <key>CFBundleIdentifier</key>
           <string>com.wooftalk.ar</string>
           <key>NSCameraUsageDescription</key>
           <string>WoofTalk AR uses camera to show the real world and detect dogs.</string>
           <key>NSMicrophoneUsageDescription</key>
           <string>WoofTalk AR uses microphone to listen for dog barks.</string>
           <key>NSMotionUsageDescription</key>
           <string>WoofTalk AR uses motion data to understand your position in space.</string>
           <key>UISupportedInterfaceOrientations</key>
           <array>
               <string>UIInterfaceOrientationLandscapeLeft</string>
               <string>UIInterfaceOrientationLandscapeRight</string>
           </array>
           <key>UIApplicationSceneManifest</key>
           <dict>
               <key>UIApplicationSupportsMultipleScenes</key>
               <false/>
           </dict>
       </dict>
       </plist>
       ```
      
    3. Link entitlements in Xcode project:
       - Add `WoofTalkAR.entitlements` to Code Signing Entitlements build setting
       - Enable Camera, Microphone, Motion capabilities in Xcode capabilities tab
  </action>
  <done>
    - Entitlements file exists with ARKit, camera, microphone permissions
    - Info.plist contains all required usage description keys
    - Project file references entitlements correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Swift Package dependencies</name>
  <files>Package.swift, WoofTalkAR/App.swift (add supabase import)
  </files>
  <read_first>
    - Read: .planning/research/STACK.md for Supabase Swift SDK version
    - Reference: Empirical evidence from Edge Functions confirms Supabase v2 API
  </read_first>
  <acceptance_criteria>
    - Package.swift contains Supabase dependency from version 2.0.0
    - App.swift imports Supabase
    - swift build resolves dependencies without errors
  </acceptance_criteria>
  <verify>
    <automated>
      test -f Package.swift
      grep -q "Supabase" Package.swift
      grep -q "from: \"2.0.0\"" Package.swift
      grep -q "import Supabase" WoofTalkAR/App.swift
    </automated>
  </verify>
  <action>
    1. Create `Package.swift` in project root:
       ```swift
       // swift-tools-version:5.9
       import PackageDescription
       
       let package = Package(
           name: "WoofTalkAR",
           platforms: [
               .visionOS(.v1)
           ],
           dependencies: [
               .package(url: "https://github.com/supabase/supabase-swift.git", from: "2.0.0"),
               .package(url: "https://github.com/apple/swift-protobuf.git", from: "1.20.0")
           ],
           targets: [
               .target(
                   name: "WoofTalkAR",
                   dependencies: [
                       .product(name: "Supabase", package: "supabase-swift")
                   ]
               )
           ]
       )
       ```
      
    2. Update `WoofTalkAR/App.swift` to import Supabase:
       ```swift
       import SwiftUI
       import Supabase
       
       @main
       struct WoofTalkAR: App {
           @StateObject private var supabaseClient = SupabaseClient(
               supabaseURL: URL(string: ProcessInfo.processInfo.environment["SUPABASE_URL"] ?? "")!,
               supabaseKey: ProcessInfo.processInfo.environment["SUPABASE_ANON_KEY"] ?? ""
           )
           
           var body: some Scene {
               WindowGroup {
                   ContentView()
                       .environmentObject(supabaseClient)
               }
           }
       }
       ```
      
    3. Create `.xcconfig` files for environment variables (or instruct user to set in Xcode):
       - `WoofTalkAR/Config/Secrets.xcconfig`:
         ```
         SUPABASE_URL = your-project-ref.supabase.co
         SUPABASE_ANON_KEY = your-anon-key
         ```
       - Reference in project: `$(SRCROOT)/WoofTalkAR/Config/Secrets.xcconfig`
  </action>
  <done>
    - Package.swift with Supabase dependency added
    - App.swift initializes SupabaseClient with environment variables
    - swift build (or xcodebuild) resolves all dependencies
    - Supabase import available in codebase
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify project builds on Vision Pro simulator</name>
  <files>WoofTalkAR.xcodeproj
  </files>
  <read_first>
    - Read: Entire project file to ensure all settings are correct
    - Reference: Xcode build system requirements
  </read_first>
  <acceptance_criteria>
    - Project builds successfully on Vision Pro simulator target
    - No linker errors (all frameworks including RealityKit, ARKit, SwiftUI found)
    - Binary produced and can be launched
  </acceptance_criteria>
  <verify>
    <automated>
      xcodebuild -scheme WoofTalkAR -destination 'platform=visionOS Simulator,name=Apple Vision Pro' -quiet build 2>&1 | grep -E "(error|warning|BUILD SUCCEEDED)" || echo "Build command executed"
      test -d build/Release-iphoneos/WoofTalkAR.app 2>/dev/null || test -d build/Debug-iphonesimulator/WoofTalkAR.app 2>/dev/null || echo "Build directory may be in different location"
    </automated>
  </verify>
  <action>
    1. Clean build folder: `xcodebuild clean -scheme WoofTalkAR`
    2. Build for Vision Pro simulator: 
       `xcodebuild -scheme WoofTalkAR -destination 'platform=visionOS Simulator,name=Apple Vision Pro' build`
    3. If build fails, diagnose and fix missing settings:
       - Check deployment target: set to visionOS 1.0
       - Check architectures: arm64
       - Check framework search paths include Swift packages
       - Check entitlements file is in Copy Bundle Resources
    4. Verify build artifacts exist in standard locations
    5. Document any manual Xcode steps required in `.planning/phases/38-ar-foundation/38-01-SETUP.md`:
       - Team ID configuration
       - Environment variable setup
       - Signing & capabilities manual configuration
  </action>
  <done>
    - Project builds successfully on Vision Pro simulator target
    - No linker errors (all frameworks including RealityKit, ARKit, SwiftUI found)
    - Binary produced and can be launched (may crash at runtime if runtime issues, but build succeeds)
    - Manual setup steps documented for user
  </done>
</task>

</tasks>

<verification>
Run verification suite:

1. Project structure check:
   - `test -d WoofTalkAR.xcodeproj`
   - `test -f WoofTalkAR/App.swift`
   - `test -f WoofTalkAR/ContentView.swift`
   - `test -f WoofTalkAR/Info.plist`
   - `test -f WoofTalkAR/Entitlements/WoofTalkAR.entitlements`
   - `test -f Package.swift`

2. Build verification:
   - `xcodebuild -scheme WoofTalkAR -destination 'platform=visionOS Simulator,name=Apple Vision Pro' build` exits 0

3. Entitlements verification:
   - `grep -q "com.apple.developer.arkit" WoofTalkAR/Entitlements/WoofTalkAR.entitlements`
   - `grep -q "com.apple.security.device.camera" WoofTalkAR/Entitlements/WoofTalkAR.entitlements`
   - `grep -q "com.apple.security.device.microphone" WoofTalkAR/Entitlements/WoofTalkAR.entitlements`

4. Info.plist verification:
   - `grep -q "NSCameraUsageDescription" WoofTalkAR/Info.plist`
   - `grep -q "NSMicrophoneUsageDescription" WoofTalkAR/Info.plist`
   - `grep -q "NSMotionUsageDescription" WoofTalkAR/Info.plist`

5. Dependencies verification:
   - `swift package resolve` (if using SwiftPM) completes successfully
   - Or: `grep -q "Supabase" Package.swift`

All checks must pass before proceeding to Wave 2.
</verification>

<success_criteria>
**AR-01 is complete when:**
- ✅ Vision Pro Xcode project exists with valid structure
- ✅ All required entitlements configured (ARKit, camera, microphone, motion)
- ✅ Info.plist contains all usage description keys
- ✅ Swift packages (Supabase) added and resolved
- ✅ Project builds successfully on Vision Pro simulator (no compilation errors)
- ✅ ARView with WorldTrackingConfiguration displays onscreen (even if blank passthrough)

**Exit criteria:** Build succeeds, app launches to blank AR view (black screen acceptable if simulation limitations). Ready for dog bark detection implementation in Wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/38-ar-foundation/38-01-SUMMARY.md` summarizing:
- Project structure created
- Entitlements configured
- Dependencies added
- Build results (success/failure and any manual steps needed)
- Files created/modified with brief descriptions
- Any issues that need user resolution (team ID, signing)
</output>
