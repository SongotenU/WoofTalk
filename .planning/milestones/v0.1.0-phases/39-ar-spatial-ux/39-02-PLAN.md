---
phase: 39-ar-spatial-ux
plan: 02
type: execute
wave: 2
depends_on:
  - "39-01"
files_modified:
  - WoofTalkAR/Views/TranslationBubble.swift
  - WoofTalkAR/ARCoordinator.swift
  - WoofTalkAR/Services/EntityPool.swift (optional)
requirements:
  - AR-09
  - AR-10
autonomous: true
must_haves:
  truths:
    - "Dynamic font scaling based on distance"
    - "Contrast enhancement (drop shadow/outline) for distant bubbles"
    - "Background opacity adjustment"
    - "90 FPS maintained with 3+ active bubbles"
    - "Text mesh caching implemented"
    - "Entity pooling (optional, if needed for FPS)"
  artifacts:
    - path: "WoofTalkAR/Views/TranslationBubble.swift"
      provides: "Distance-based configuration: font, opacity, shadows"
      min_lines: 120
      contains:
        - "configureForDistance"
        - "fontSize"
    - path: "WoofTalkAR/Services/EntityPool.swift"
      provides: "Optional object pool for bubble entities"
      min_lines: 50
    - path: "WoofTalkAR/ARCoordinator.swift"
      provides: "FPS monitoring"
      contains:
        - "monitorFPS"
  key_links:
    - from: "ARPlacementEngine.swift"
      to: "TranslationBubble.swift"
      via: "distance parameter"
      pattern: "configureForDistance"
---

<objective>
Optimize readability and performance: dynamic font scaling, contrast adjustments, text mesh caching, entity pooling, FPS profiling.

<objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/39-ar-spatial-ux/39-CONTEXT.md

Readability: font scales with distance; opacity varies; drop shadow for >5m.
Performance: target 90 FPS with 3 bubbles; use pooling and caching; profile FPS.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic font scaling and contrast</name>
  <description>
    Add configureForDistance(_:) to TranslationBubble. Scale font: base * (distance/2), clamp [0.02, 0.1]. Opacity: 0.85 (≤3m) else 0.95. Optional drop shadow.
  </description>
  <acceptance>
    - Font size changes with distance
    - Background opacity varies
    - Visual improvements visible
  </acceptance>
</task>

<task type="auto">
  <name>Task 2: Text mesh caching</name>
  <description>
    Implement static NSCache in TranslationBubble: key = text+font+size, value = MeshResource. Check cache before generateText.
  </description>
  <acceptance>
    - Cache reduces repeated generateText calls
    - No memory leaks
  </acceptance>
</task>

<task type="auto">
  <name>Task 3: Entity pooling (optional)</name>
  <description>
    Create EntityPool actor with pre-allocated bubble entities (capacity 5). ARCoordinator uses acquire/release instead of new/remove.
  </description>
  <acceptance>
    - Pool provides entities
    - ARCoordinator integrates pooling
    - Entity state reset on release
  </acceptance>
</task>

<task type="auto">
  <name>Task 4: FPS profiling</name>
  <description>
    Add FPS monitor to ARCoordinator; log every 5s. Manual test with 0-3 bubbles; document results. If FPS < 85, iterate optimizations.
  </description>
  <acceptance>
    - FPS logging works
    - Profiling data collected
    - Meets target or mitigation documented
  </acceptance>
</task>

</tasks>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<success_criteria>
- [ ] All 4 tasks completed
- [ ] TranslationBubble distance configuration
- [ ] Text mesh caching
- [ ] FPS ≥ 85 with 3 bubbles
- [ ] SUMMARY.md created
</success_criteria>
