---
phase: 39-ar-spatial-ux
plan: 01
type: execute
wave: 1
depends_on:
  - "38-03"
files_modified:
  - WoofTalkAR/Services/ARPlacementEngine.swift
  - WoofTalkAR/ARCoordinator.swift
  - WoofTalkAR/Views/TranslationBubble.swift (minor updates)
requirements:
  - AR-07
  - AR-08
autonomous: true
must_haves:
  truths:
    - "Gaze-based dog position estimation using ARKit raycast"
    - "Bubble placement with distance clamping (1-10m)"
    - "Billboarding maintained"
    - "Occlusion checks prevent placing bubbles behind geometry"
    - "ARCoordinator delegates placement to ARPlacementEngine"
    - "Fallback positioning when raycast fails"
  artifacts:
    - path: "WoofTalkAR/Services/ARPlacementEngine.swift"
      provides: "Placement logic: gaze estimation, clamping, occlusion, fallback"
      min_lines: 100
      contains:
        - "raycast"
        - "estimateDogPosition"
        - "isPositionVisible"
        - "clampDistance"
    - path: "WoofTalkAR/ARCoordinator.swift"
      provides: "Integration of placement engine into bubble lifecycle"
      contains:
        - "ARPlacementEngine"
        - "placeBubble"
    - path: "WoofTalkAR/Views/TranslationBubble.swift"
      provides: "Bubble positioning updated from placement engine"
      pattern: "position"
  key_links:
    - from: "ARPlacementEngine.swift"
      to: "ARCoordinator.swift"
      via: "shared instance"
      pattern: "ARPlacementEngine\\.shared"
    - from: "ARCoordinator.swift"
      to: "ARPlacementEngine.swift"
      via: "calls placeBubble"
      pattern: "placeBubble"
---

<objective>
Implement gaze-based dog position estimation and intelligent bubble placement engine to replace fixed 2m positioning.

**Output:**
- `ARPlacementEngine` singleton actor with raycast-based positioning, distance clamping (1-10m), occlusion checks, and fallback
- `ARCoordinator.showBubble` uses placement engine instead of hardcoded offset
- Fallback: if raycast fails, place at fixed distance along gaze (5m average with jitter)

<objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-ar-spatial-ux/39-CONTEXT.md

Phase 38 baseline: bubbles at cameraPos + forward * 2.0
Need: gaze-aware positioning with environmental awareness.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ARPlacementEngine with raycast positioning</name>
  <description>
    Implement singleton actor:
    - estimateDogPosition(from:in:) using ARView.raycast along camera forward
    - Fallback: cameraPos + forward * random(4,6) if no hit
    - clampDistance(_:from:) enforcing 1-10m
    - Unit tests for math and edge cases
  </description>
  <acceptance>
    - ARPlacementEngine compiles
    - Raycast path returns world position from hit
    - Fallback activates when raycast returns empty
    - Distance clamping verified
  </acceptance>
</task>

<task type="auto">
  <name>Task 2: Implement occlusion check</name>
  <description>
    Add isPositionVisible(from:to:in:) — raycast from camera to target; if hit distance < target distance, position is occluded.
  </description>
  <acceptance>
    - Returns false when geometry blocks line-of-sight
    - Returns true when clear
    - Edge: exact hit counts as visible
  </acceptance>
</task>

<task type="auto">
  <name>Task 3: Integrate into ARCoordinator</name>
  <description>
    Replace hardcoded 2m with: let pos = ARPlacementEngine.shared.placeBubble(...). Fallback to old method if nil.
  </description>
  <acceptance>
    - ARCoordinator imports ARPlacementEngine
    - showBubble uses placement engine
    - Existing lifecycle unchanged
  </acceptance>
</task>

<task type="auto">
  <name>Task 4: Update TranslationBubble positioning</name>
  <description>
    Ensure TranslationBubble accepts external position; modify initializer if needed to take position parameter.
  </description>
  <acceptance>
    - TranslationBubble init includes position: SIMD3<Float>
    - ARCoordinator passes computed position
  </acceptance>
</task>

</tasks>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<success_criteria>
- [ ] All 4 tasks completed
- [ ] ARPlacementEngine.swift created
- [ ] ARCoordinator uses placement engine
- [ ] TranslationBubble positioning dynamic
- [ ] Unit tests pass
- [ ] SUMMARY.md created
</success_criteria>
