---
phase: 41-vr-environments-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 41-01
  - 41-02
files_modified:
  - vr-quest/Assets/Scripts/UI/ComfortSettings.cs
  - vr-quest/Assets/Scripts/UI/SettingsMenu.cs
  - vr-quest/Assets/Prefabs/SettingsMenu.prefab
  - vr-quest/Assets/Scripts/Comfort/Vignette.cs
  - vr-quest/Assets/Scripts/Comfort/HeadLockedUI.cs
autonomous: true
requirements:
  - VR-10
  - VR-11
  - VR-12
must_haves:
  truths:
    - "Vignette effect activates during VR movement to reduce motion sickness"
    - "Head-locked UI moves with the camera, stays readable"
    - "Settings menu allows volume, bubble opacity, and comfort toggles"
  artifacts:
    - path: "vr-quest/Assets/Scripts/Comfort/Vignette.cs"
      provides: "Dynamic vignette overlay during locomotion"
      exports:
        - "SetIntensity(float)"
        - "EnableDuringMovement(bool)"
    - path: "vr-quest/Assets/Scripts/UI/SettingsMenu.cs"
      provides: "VR settings panel with volume, opacity, comfort toggles"
      exports:
        - "Show()"
        - "Hide()"
        - "SetVolume(float)"
        - "SetBubbleOpacity(float)"
---

<objective>

Implement motion sickness mitigation (vignette, head-locked UI), settings menu (volume, bubble opacity, comfort), and user testing framework.

</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create motion sickness mitigation — vignette during movement, head-locked UI, comfort mode (VR-10)</name>
  <files>
    - vr-quest/Assets/Scripts/Comfort/Vignette.cs
    - vr-quest/Assets/Scripts/Comfort/HeadLockedUI.cs
  </files>
  <action>
    A. Create Vignette.cs:
       - MonoBehaviour that applies a dark overlay around edges during movement
       - [SerializeField] private Canvas vignetteCanvas
       - [SerializeField] private Image vignetteImage
       - [SerializeField] private float maxIntensity = 0.8f
       - Public void SetIntensity(float value): vignetteImage.color = new Color(0, 0, 0, value * maxIntensity)
       - Public bool EnableDuringMovement — when true, vignette activates during locomotion
       - Private bool _isMoving — track user velocity (via OVRCameraRig velocity)
       - Update(): if EnableDuringMovement and _isMoving: SetIntensity based on velocity magnitude; else: SetIntensity(0)

    B. Create HeadLockedUI.cs:
       - MonoBehaviour that keeps a UI element locked to the camera's view
       - [SerializeField] private Transform cameraTarget — OVRCameraRig centerEyeAnchor
       - [SerializeField] private Vector3 offset = new Vector3(0, -0.3f, 0.5f) — slightly below center
       - LateUpdate(): transform.position = cameraTarget.position + cameraTarget.forward * offset.z + cameraTarget.up * offset.y
       - transform.rotation = cameraTarget.rotation
       - Note: Used for FPS display, crosshair, or important notifications
  </action>
  <acceptance_criteria>
    - Vignette.cs has SetIntensity, EnableDuringMovement, velocity-based activation
    - HeadLockedUI.cs has LateUpdate with camera-relative positioning
    - Both use correct Unity rendering APIs
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "SetIntensity" vr-quest/Assets/Scripts/Comfort/Vignette.cs && grep -q "EnableDuringMovement" vr-quest/Assets/Scripts/Comfort/Vignette.cs && grep -q "cameraTarget" vr-quest/Assets/Scripts/Comfort/HeadLockedUI.cs && echo "VR-10 verified" || echo "VR-10 FAILED"
    </automated>
  </verify>
  <done>
    - Vignette effect intensity scales with movement velocity
    - HeadLockedUI keeps critical UI elements in camera view at all times
    - Comfort mode configurable via enable/disable flag
  </done>
</task>

<task type="auto">
  <name>Task 2: Create settings menu with volume, bubble opacity, and comfort toggles (VR-11)</name>
  <files>
    - vr-quest/Assets/Scripts/UI/SettingsMenu.cs
    - vr-quest/Assets/Prefabs/SettingsMenu.prefab
  </files>
  <read_first>
    - vr-quest/Assets/Scripts/UI/VRMenu.cs (existing menu system)
  </read_first>
  <action>
    A. Create SettingsMenu.cs:
       - MonoBehaviour for VR settings panel
       - [SerializeField] private Slider volumeSlider
       - [SerializeField] private Slider bubbleOpacitySlider
       - [SerializeField] private Toggle comfortModeToggle
       - Public void Show(): gameObject.SetActive(true)
       - Public void Hide(): gameObject.SetActive(false)
       - Public void SetVolume(float value): AudioListener.volume = value
       - Public void SetBubbleOpacity(float value): Update BubbleManager opacity (reference to existing BubbleManager)
       - Public void ApplySettings(): Save to PlayerPrefs (or custom settings manager)
       - Awake(): Load saved settings from PlayerPrefs

    B. Create SettingsMenu.prefab:
       - World-space Canvas (400x300, same pattern as VRMenu)
       - Volume slider (0.0 - 1.0)
       - Bubble opacity slider (0.0 - 1.0)
       - Comfort mode toggle button
       - Close button
       - TMP labels for each control
  </action>
  <acceptance_criteria>
    - SettingsMenu.cs has Show, Hide, SetVolume, SetBubbleOpacity methods
    - SettingsMenu.prefab exists with sliders and toggle
    - Settings load from PlayerPrefs on Awake
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "SetVolume" vr-quest/Assets/Scripts/UI/SettingsMenu.cs && grep -q "SetBubbleOpacity" vr-quest/Assets/Scripts/UI/SettingsMenu.cs && grep -q "PlayerPrefs" vr-quest/Assets/Scripts/UI/SettingsMenu.cs && grep -q "SettingsMenu" vr-quest/Assets/Prefabs/SettingsMenu.prefab && echo "VR-11 verified" || echo "VR-11 FAILED"
    </automated>
  </verify>
  <done>
    - SettingsMenu provides volume, bubble opacity, and comfort mode controls
    - Settings persist across sessions via PlayerPrefs
    - Menu show/hide accessible from VRMenu button
  </done>
</task>

<task type="auto">
  <name>Task 3: Create user testing framework and session tracking (VR-12)</name>
  <files>
    - vr-quest/Assets/Scripts/Testing/TestSession.cs
    - vr-quest/Assets/Scripts/Testing/FPSLogger.cs
  </files>
  <action>
    A. Create TestSession.cs:
       - MonoBehaviour for tracking VR session metrics
       - Public float SessionDuration { get; }
       - Public int TotalBarksDetected { get; private set; }
       - Public int TotalBubblesShown { get; private set; }
       - Public string CurrentEnvironment { get; private set; }
       - OnEnable(): session start time, log "[TestSession] Session started"
       - OnDisable(): session end time, log "[TestSession] Session ended. Duration: {duration}s"
       - Public void RecordBark(): TotalBarksDetected++
       - Public void RecordBubble(): TotalBubblesShown++
       - Public void LogSummary(): log all metrics

    B. Create FPSLogger.cs:
       - MonoBehaviour that logs FPS every 30 seconds during testing
       - [SerializeField] private PerformanceMonitor performanceMonitor
       - Logs: "[FPSLogger] Average FPS: {avg} over last 30s"
       - Tracks: min FPS, max FPS, average FPS since session start
  </action>
  <acceptance_criteria>
    - TestSession.cs tracks session duration, bark count, bubble count, environment
    - FPSLogger.cs logs min/max/average FPS every 30 seconds
    - Both scripts compile with correct Unity API usage
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "SessionDuration" vr-quest/Assets/Scripts/Testing/TestSession.cs && grep -q "TotalBarksDetected" vr-quest/Assets/Scripts/Testing/TestSession.cs && grep -q "FPS" vr-quest/Assets/Scripts/Testing/FPSLogger.cs && echo "VR-12 verified" || echo "VR-12 FAILED"
    </automated>
  </verify>
  <done>
    - TestSession tracks key metrics for user testing sessions
    - FPSLogger provides detailed performance statistics over time
    - Framework supports iterative user testing cycles
  </done>
</task>

</tasks>

<verification>
- Vignette.cs applies dynamic overlay during movement
- HeadLockedUI.cs keeps UI in camera view
- SettingsMenu.cs provides all required settings controls
- TestSession.cs and FPSLogger.cs provide testing metrics
- Experience.unity updated with all new components
</verification>

<success_criteria>
- VR-10 satisfied: Vignette during movement, head-locked UI, comfort mode
- VR-11 satisfied: Settings menu with volume, bubble opacity, comfort toggles
- VR-12 satisfied: User testing framework with session tracking and FPS logging
</success_criteria>

<output>
Create `.planning/phases/41-vr-environments-polish/41-03-SUMMARY.md`
</output>

## PLANNING COMPLETE