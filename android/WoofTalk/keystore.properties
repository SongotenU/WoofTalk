# Keystore properties for signing release builds
# This file is gitignored — never commit real credentials

storeFile=wooftalk-release.keystore
storePassword=wooftalk123
keyAlias=wooftalk-key
keyPassword=wooftalk123
